# Trust Route 커맨드 빠른 참조

## 📦 전체 커맨드 목록 (18개)

### Git & 배포 (3개)
```bash
/commit-push [타입] "메시지"    # 커밋 + 푸시
/deploy                          # 테스트 + 배포
/quick-fix "수정내용"            # 빠른 수정 + 즉시 배포
```

### 테스트 & 검증 (7개)
```bash
/test-build                      # 빌드 검증
/preview                         # 배포 전 체크리스트
/analyze                         # 코드 품질 분석
/seo-check                       # SEO 메타태그 검증
/image-optimize                  # 이미지 최적화 분석
/performance                     # 성능 측정
/security-scan                   # 보안 스캔
```

### 맛집 데이터 (2개)
```bash
/add-restaurant                  # 새 맛집 추가
/update-menu "식당명"            # 대표 메뉴 업데이트
```

### 데이터베이스 (2개)
```bash
/db-migrate                      # Supabase 마이그레이션
/backup                          # 데이터 백업
```

### 로컬 개발 (2개)
```bash
/local-server [포트]             # 로컬 서버 실행
/clean                           # 캐시 정리
```

### 프로젝트 관리 (2개)
```bash
/status                          # 프로젝트 상태
/help                            # 도움말
```

---

## 🚀 일반적인 워크플로우

### 1. 일상적인 개발
```bash
/local-server                    # 서버 시작
# 코드 작성...
/preview                         # 배포 전 체크
/commit-push feat "Add feature"  # 커밋 + 푸시
```

### 2. 긴급 버그 수정
```bash
# 코드 수정...
/quick-fix "Fix critical bug"   # 즉시 배포
```

### 3. 배포 전 완전 체크
```bash
/test-build                      # 기본 검증
/seo-check                       # SEO 검증
/performance                     # 성능 분석
/security-scan                   # 보안 스캔
/preview                         # 종합 체크
/deploy                          # 배포
```

### 4. 주간 점검
```bash
/status                          # 프로젝트 상태
/analyze                         # 코드 품질
/backup                          # 백업
/clean                           # 정리
```

---

## 💡 체크리스트

### ✅ 배포 전
- [ ] `/test-build` - JavaScript 구문 검사
- [ ] `/preview` - 7가지 항목 체크
- [ ] `/seo-check` - SEO 점수 80% 이상
- [ ] `/security-scan` - 취약점 0개

### ✅ 맛집 추가 시
- [ ] `/add-restaurant` - 정보 입력
- [ ] `data.js` 편집 - JSON 데이터 추가
- [ ] `/test-build` - 검증
- [ ] `/commit-push feat "Add restaurant"` - 커밋

### ✅ 대표 메뉴 업데이트 시
- [ ] 네이버 플레이스 리뷰 확인
- [ ] `/update-menu "식당명"` - 메뉴 업데이트
- [ ] `/commit-push fix "Update menu"` - 커밋

### ✅ 주간 점검
- [ ] `/status` - 상태 확인
- [ ] `/analyze` - 코드 품질
- [ ] `/performance` - 성능 점수 7점 이상
- [ ] `/security-scan` - 보안 체크
- [ ] `/backup` - 백업

---

## 🔥 자주 사용하는 커맨드 Top 5

1. **`/commit-push`** - 매일 사용 (커밋 + 푸시)
2. **`/preview`** - 배포 전 필수
3. **`/local-server`** - 개발 시 필수
4. **`/quick-fix`** - 긴급 수정
5. **`/status`** - 상태 확인

---

## 📖 자세한 가이드

- **전체 가이드**: `.claude/commands/README.md`
- **프로젝트 가이드**: `CLAUDE.md`
- **도움말**: `/help`
